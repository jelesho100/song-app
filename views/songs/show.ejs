<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= song.name %></title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>

  <div class="page-container">
    <h2><%= song.name %> by <%= song.artist %></h2>

    <div class="song-actions">
      <a class="button-link" href="/users/<%= user._id %>/songs/<%= song._id %>/edit">Edit song</a>

      <form
        action="/users/<%= user._id %>/songs/<%= song._id %>?_method=DELETE"
        method="POST"
        style="display:inline;">
        <button class="delete-button" type="submit">Delete this song</button>
      </form>
    </div>
  </div>
</body>
</html>
